// src/actions.ts - iOS-specific actions using Appium/WebdriverIO
export async function tap(elementOrCoords: any) {
  console.log("Placeholder: Performing tap on", elementOrCoords);
}

export async function type(text: string) {
  console.log("Placeholder: Typing text:", text);
}

// More to come: swipe, launchApp (by bundleId), getScreenshot, etc.
