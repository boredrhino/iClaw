// src/workflow.ts - Handles JSON workflow files (multi-app goals)
export function parseWorkflow(json: any) {
  console.log("Placeholder: Parsing workflow:", json.name);
  // Future: validate steps, run sequentially with LLM guidance
}
