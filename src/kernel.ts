// src/kernel.ts - Main agent execution loop for iClaw (iOS version)
console.log("iClaw kernel starting...");
console.log("Placeholder: Will initialize Appium session, run reasoning loop, execute actions.");
// Future: import driver, get state, call LLM, act, repeat until goal done
